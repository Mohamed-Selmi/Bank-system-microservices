type User{
    id:String!
    name: String!
    CIN: String!
    account_id: String!
}

type Account{
    id: String!
    code: String!
    balance: Float!
    user_id: String!
}

type Query{
    user(id: String!): User
    users:[User]
    account(id: String!): Account
    accounts:[Account]
}